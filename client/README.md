# Carevyn Hospital Management System

Local development

1. Install dependencies with `npm install`.
2. Start the frontend with `npm run dev`.
3. The frontend uses `http://localhost:3700` for the local backend by default.
